let showdown  = require('showdown');
let _ = require('underscore');
let fs = require('fs');
let templateHtml = fs.readFileSync(process.argv[2], 'utf-8');

const run = () => {
    try {

    } catch (err) {
    
    }
    console.log(html)
}

